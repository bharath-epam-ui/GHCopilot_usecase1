# Vercel KV (Upstash Redis) — copy these from: Vercel Dashboard → Settings → Environment Variables
# Without these, the app uses in-memory storage (fine for local dev only)
KV_REST_API_URL=
KV_REST_API_TOKEN=
